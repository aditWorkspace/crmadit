---
phase: uninterested-lead-filter
task: 5
total_tasks: 5
status: completed
last_updated: 2026-04-08T01:45:39.109Z
---

<current_state>
All implementation and cleanup work is COMPLETE. Code is written, tested, and the live database has been cleaned. Changes are uncommitted — need to commit and merge to main.
</current_state>

<completed_work>

- Task 1: Create reply intent classifier - Done (`src/lib/gmail/reply-classifier.ts`)
  - AI-based classifier using free model (google/gemma-4-26b-a4b-it:free)
  - Retries on 429 with exponential backoff (4s * attempt)
  - Defaults to 'interested' on any failure (safe default)
- Task 2: Integrate classifier into email sync flow - Done (`src/lib/gmail/sync.ts`)
  - Added `classifyReplyIntent()` call in Path 2 before new lead creation (line ~402)
  - Added `filtered_not_interested` counter to `SyncResult`
  - Only filters NEW lead creation, not updates to existing leads
- Task 3: Fix broken QWEN_FREE_MODEL constant - Done (`src/lib/constants.ts`)
  - Was `qwen/qwen3-6b:free` (404 on OpenRouter — model no longer exists)
  - Changed to `google/gemma-4-26b-a4b-it:free`
  - This also fixes email summarization + auto-followup AI decisions that were silently failing
- Task 4: Create cleanup API route - Done (`src/app/api/leads/cleanup-uninterested/route.ts`)
  - POST endpoint with ?dry_run=true support
  - Note: HTTP timeout makes this impractical for large batches
- Task 5: Run cleanup against live database - Done (via `scripts/cleanup-uninterested.mjs`)
  - 113 leads started in 'replied' stage → 23 archived as not interested, 90 kept
  - All 3 user-reported leads confirmed archived (Lapis Labs, SkyConnect, morriganAI)
  - Used hybrid approach: keyword patterns (Phase 1, instant) + AI classification (Phase 2)
  - Used paid model (google/gemma-4-26b-a4b-it, ~$0.01) for batch; free tier too rate-limited
</completed_work>

<remaining_work>

- Commit all changes to git
- Merge branch to main (or create PR)
- Consider removing the one-time `scripts/cleanup-uninterested.mjs` after merge
- The cleanup API route (`/api/leads/cleanup-uninterested`) can stay for future use
</remaining_work>

<decisions_made>

- Used AI classification over pure keyword matching for the ongoing sync filter — keywords miss nuanced cases (e.g., "I think your AI email tool is bad if this is the email it matched morriganAI to!")
- Classifier defaults to 'interested' on failure — false negative (filtering real lead) is worse than false positive (junk lead needing manual archive)
- Only classify on NEW lead creation, not existing lead updates — ongoing conversations shouldn't be re-evaluated
- Used hybrid keyword+AI approach for batch cleanup — free tier rate limits (429s every few calls) made pure AI impractical for 113 leads
- Changed QWEN_FREE_MODEL from dead qwen model to google/gemma-4-26b — discovered during testing that the old model was 404ing, causing ALL AI features using it to silently fail
</decisions_made>

<blockers>
- None — all work complete
</blockers>

<context>
The user showed 3 screenshots of leads with clearly hostile/dismissive replies ("nice try", "wrong company", "AI tool is bad") that were stuck in "In Dialogue" (replied) stage. The root issue was that email sync created a lead from ANY inbound reply, regardless of sentiment. The fix has two parts: (1) prevent future junk leads at sync time, (2) retroactively clean existing ones.

Key discovery: the free AI model constant was broken, which means email summarization and auto-followup decisions were also silently failing. This was a bonus fix.

Files modified:
- `src/lib/gmail/reply-classifier.ts` (NEW) — AI intent classifier
- `src/lib/gmail/sync.ts` (MODIFIED) — integrated classifier before lead creation
- `src/lib/constants.ts` (MODIFIED) — fixed dead model ID
- `src/app/api/leads/cleanup-uninterested/route.ts` (NEW) — cleanup API route
- `scripts/cleanup-uninterested.mjs` (NEW) — one-time batch cleanup script
</context>

<next_action>
Start with: Commit all changes and create PR or merge to main. The cleanup script can be deleted after merge if desired.
</next_action>
